# Examples Context

## Usage

- Run these scripts from the repository root, e.g., `python3 src/examples/insect_simulation_example.py`.
- These files are excellent references for how to instantiate Agents and Environments.

# Utilities Context

## Usage

- Prefer using these utilities over implementing custom path logic or array manipulators.
- `animation.py` is heavy; use only when visual debugging or presentation extraction is needed.
